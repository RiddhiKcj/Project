<div class="container content-page">
      <h3><strong><u>Articles</u></strong></h3>
      <div class="p-b-40">
	      <table class="table table-striped table-hover">
              <% @users.each do |user| %>
                <% if user.myapis.any? %>
                    <% user.myapis.each do |article| %>
                        <tr>
                        <td><strong><%= article.title %></strong></td>
                        <td><%= article.body %></td>
                        <td>  ~ by  <%= user.username.humanize %></td>   
                        </tr>
                    <% end %>
                <% end %>
            <% end %>   
            </table>
        </div>
</div>